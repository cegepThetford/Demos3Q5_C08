<Window x:Class="Demos3Q5_C08_UI.Trigger1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trigger1" Height="300" Width="300">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!--<Style x:Key="forExemple">
            <Style.Triggers>
                <Trigger Property="Button.IsMouseOver" Value="True">
                    <Setter Property="Button.Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>-->
    </Window.Resources>
    <StackPanel x:Name="stack1">
        <!--<Button x:Name="bt1"
                Content="Put your mouse over me!"
                Style="{StaticResource forExemple}"
                />-->
        <TextBlock>New York</TextBlock>
        <TextBlock>Londres</TextBlock>
        <TextBlock>Thetford Mines</TextBlock>
    </StackPanel>
</Window>
